[package]
name = "mdparcel-desktop"
version = "0.1.0"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2.5", features = [] }

[dependencies]
mdparcel = { path = "../.." }
tauri = { version = "2.11", features = [] }
tauri-plugin-dialog = "2.7"
